# Frontend (Vite dev server)
VITE_PORT=5173

# Backend API URL (proxy target)
VITE_API_URL=http://localhost:8080

# Access token lifetime in ms — must match JWT_SECRET_ACCESS_TOKEN_EXP on backend (default: 15m = 900000ms)
VITE_ACCESS_TOKEN_EXP_MS=900000
